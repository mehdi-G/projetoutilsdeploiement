#!/bin/bash

# Conteneur 1: Serveur Nginx
docker run -d --name web1 nginx

# Conteneur 2: Serveur Nginx
docker run -d --name web2 nginx

# Conteneur 3: Serveur Nginx
docker run -d --name web3 nginx

echo "3 conteneurs sont démarrés avec succès !"
